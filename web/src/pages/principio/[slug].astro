---
// Página por principio activo: todos los productos del grupo ordenados por
// precio, marcando genéricos vs. marca.
//
// getStaticPaths() vacío a propósito: depende de `drugs` poblado por el
// normalizador (fase 3, en pausa). TODO (fase 6 del Orden de construcción).
export async function getStaticPaths() {
  return [];
}
---

<p>TODO: página de principio activo</p>
